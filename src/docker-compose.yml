version: '3.7'

services:
  apiserver:
    build:
      context: .
      dockerfile: server/Dockerfile
    command: uvicorn faq_server:app --reload --workers 1 --host 0.0.0.0 --port 8000
    ports:
      - 8001:8000
